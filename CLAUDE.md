# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**ally** — a social experiment. The repository is in early stages with no code implemented yet.

The `.gitignore` is configured for a Node.js/TypeScript project and references common framework outputs (`.next`, `.nuxt`, `dist`), suggesting a modern JavaScript/TypeScript web application is planned.

## Getting Started

No `package.json`, build tooling, or framework has been set up yet. When scaffolding begins, update this file with:

- Install command (e.g., `npm install` or `yarn`)
- Dev server command
- Build command
- Lint/test commands
